import "animate.css"

const prefix = "animate__"

// animations
const animate = `${prefix}animated`
const fadeInLeft = `${prefix}fadeInLeft`
const fadeInDown = `${prefix}fadeInDown`
const fadeInUp = `${prefix}fadeInUp`
const flash = `${prefix}flash`


// animation timing
const infinite = `${prefix}infinite`
const slower = `${prefix}slower`

export { animate, fadeInLeft, fadeInDown, fadeInUp, flash, infinite, slower }